# Python Tooling

Use this file to keep local commands, CI, and editor expectations aligned after copying the template.

Recommended sections:

- Environment and dependency bootstrap
- Validation and test commands
- Formatting and static-analysis expectations
- Generated-code or pre-commit steps contributors must run

Recommended follow-up after copy:

- Replace placeholder command guidance with the destination repo's real tooling.
- Confirm the install, validation, and test commands still match the checked-in project config and CI.
- Keep `AGENTS.md`, contributor docs, and automation aligned when tooling changes.
- Document pre-commit hooks, formatting expectations, and any generated code steps that contributors must run locally.
