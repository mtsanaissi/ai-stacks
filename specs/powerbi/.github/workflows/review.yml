name: Power BI Review Template

on:
  workflow_dispatch:

jobs:
  review-template:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Replace with metadata checks, export validation, or review automation as needed."
