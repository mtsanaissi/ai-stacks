# Deployment

Use this file as the project-facing record of how the copied Next.js app is deployed.

Recommended sections:

- Hosting platform and runtime targets
- Required environment variables
- Build-time versus runtime configuration
- Preview versus production differences
- Release, rollback, and incident notes

Keep the real deployment rules here after copying the template rather than leaving them implied in CI or agent instructions.
