# App Router

- Use the App Router as the default project structure.
- Keep route ownership clear by colocating data and UI under the route segment that owns them.
- Prefer async server components for initial data loading.
